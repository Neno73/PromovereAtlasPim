/**
 * Meilisearch Service Registration
 *
 * Exports the Meilisearch service for Strapi auto-registration.
 * Service is accessible via strapi.service('meilisearch')
 */

import meilisearchService from './meilisearch-service';

export default meilisearchService;
